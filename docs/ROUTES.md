# Gatekeeper Route Reference

This document is auto-generated by `scripts/generate_routes_doc.py` from the live `GoogleModule` subclasses. Do not edit by hand.

## Summary

| Module | Display name | Routes | Default-enabled | Required OAuth scopes |
|---|---|---:|---:|---|
| `calendar` | Google Calendar | 38 | 13 | `https://www.googleapis.com/auth/calendar`<br>`https://www.googleapis.com/auth/calendar.events` |
| `drive` | Google Drive | 83 | 37 | `https://www.googleapis.com/auth/drive`<br>`https://www.googleapis.com/auth/spreadsheets`<br>`https://www.googleapis.com/auth/documents`<br>`https://www.googleapis.com/auth/presentations` |
| `gmail` | Google Gmail | 53 | 16 | `https://www.googleapis.com/auth/gmail.modify`<br>`https://www.googleapis.com/auth/gmail.send`<br>`https://www.googleapis.com/auth/gmail.compose`<br>`https://www.googleapis.com/auth/gmail.settings.basic` |

**Total routes:** 174  
**Default-enabled routes:** 66

## Module: `calendar` — Google Calendar

View, create, and manage calendar events

### Required OAuth scopes

- `https://www.googleapis.com/auth/calendar`
- `https://www.googleapis.com/auth/calendar.events`

### Routes (38)

#### `calendar.events.list`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/events/list`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_results": 50}`

List events in a calendar

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier (use 'primary' for default)",
      "type": "string"
    },
    "max_results": {
      "default": 20,
      "description": "Maximum number of events to return",
      "type": "integer"
    },
    "q": {
      "description": "Free text search terms to find events",
      "type": "string"
    },
    "time_max": {
      "description": "Upper bound for event end time (RFC3339)",
      "type": "string"
    },
    "time_min": {
      "description": "Lower bound for event start time (RFC3339)",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `calendar.events.get`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/events/get`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/{eventId}`
- **Enabled by default:** `Yes`

Get a specific event by ID

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "event_id": {
      "description": "Event identifier",
      "type": "string"
    }
  },
  "required": [
    "event_id"
  ],
  "type": "object"
}
```

#### `calendar.events.create`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/events/create`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events`
- **Enabled by default:** `No`

Create a new calendar event

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "description": {
      "description": "Event description",
      "type": "string"
    },
    "end": {
      "description": "End time with 'dateTime' and 'timeZone'",
      "type": "object"
    },
    "start": {
      "description": "Start time with 'dateTime' and 'timeZone'",
      "type": "object"
    },
    "summary": {
      "description": "Event title",
      "type": "string"
    }
  },
  "required": [
    "summary",
    "start",
    "end"
  ],
  "type": "object"
}
```

#### `calendar.events.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/calendar/events/update`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/{eventId}`
- **Enabled by default:** `No`

Update an existing calendar event

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "event_id": {
      "description": "Event identifier",
      "type": "string"
    },
    "summary": {
      "description": "Updated event title",
      "type": "string"
    }
  },
  "required": [
    "event_id"
  ],
  "type": "object"
}
```

#### `calendar.events.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/calendar/events/delete`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/{eventId}`
- **Enabled by default:** `No`

Delete a calendar event

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "event_id": {
      "description": "Event identifier to delete",
      "type": "string"
    }
  },
  "required": [
    "event_id"
  ],
  "type": "object"
}
```

#### `calendar.events.quick_add`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/events/quick_add`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/quickAdd`
- **Enabled by default:** `No`

Create event from natural text (e.g., 'Lunch with Alex tomorrow at noon')

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "text": {
      "description": "Natural language event description",
      "type": "string"
    }
  },
  "required": [
    "text"
  ],
  "type": "object"
}
```

#### `calendar.calendars.list`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/calendars/list`
- **Google path:** `/calendar/v3/users/me/calendarList`
- **Enabled by default:** `Yes`

List calendars on the user's calendar list

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.calendarlist.list`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/calendarlist/list`
- **Google path:** `/calendar/v3/users/me/calendarList`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_results": 50}`

List calendar entries for the user

```json
{
  "properties": {
    "max_results": {
      "default": 20,
      "description": "Maximum number of calendars to return",
      "type": "integer"
    }
  },
  "type": "object"
}
```

#### `calendar.calendars.get`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/calendars/get`
- **Google path:** `/calendar/v3/calendars/{calendarId}`
- **Enabled by default:** `Yes`

Get details of a specific calendar

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier (use 'primary' for default)",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.calendars.create`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/calendars/create`
- **Google path:** `/calendar/v3/calendars`
- **Enabled by default:** `No`

Create a new calendar

```json
{
  "properties": {
    "description": {
      "description": "Description of the new calendar",
      "type": "string"
    },
    "summary": {
      "description": "Title of the new calendar",
      "type": "string"
    },
    "timezone": {
      "description": "Time zone for the calendar (e.g., 'America/Chicago')",
      "type": "string"
    }
  },
  "required": [
    "summary"
  ],
  "type": "object"
}
```

#### `calendar.calendars.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/calendar/calendars/delete`
- **Google path:** `/calendar/v3/calendars/{calendarId}`
- **Enabled by default:** `No`

Delete a calendar

```json
{
  "properties": {
    "calendar_id": {
      "description": "Calendar identifier to delete",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.freebusy.query`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/freebusy/query`
- **Google path:** `/calendar/v3/freeBusy`
- **Enabled by default:** `Yes`

Query free/busy information for calendars

```json
{
  "properties": {
    "calendar_ids": {
      "description": "Calendar IDs to query",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "time_max": {
      "description": "End of query range (RFC3339)",
      "type": "string"
    },
    "time_min": {
      "description": "Start of query range (RFC3339)",
      "type": "string"
    }
  },
  "required": [
    "time_min",
    "time_max"
  ],
  "type": "object"
}
```

#### `calendar.acl.list`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/acl/list`
- **Google path:** `/calendar/v3/calendars/{calendarId}/acl`
- **Enabled by default:** `Yes`

List calendar access control rules

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.acl.get`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/acl/get`
- **Google path:** `/calendar/v3/calendars/{calendarId}/acl/{ruleId}`
- **Enabled by default:** `Yes`

Get a specific ACL rule

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "rule_id": {
      "description": "The ACL rule ID to retrieve",
      "type": "string"
    }
  },
  "required": [
    "calendar_id",
    "rule_id"
  ],
  "type": "object"
}
```

#### `calendar.acl.create`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/acl/create`
- **Google path:** `/calendar/v3/calendars/{calendarId}/acl`
- **Enabled by default:** `No`

Create an ACL rule (share a calendar)

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "role": {
      "description": "Role to grant: reader, writer, or owner",
      "type": "string"
    },
    "scope_type": {
      "description": "Scope type: user, group, or default",
      "type": "string"
    },
    "scope_value": {
      "description": "Email address or group email for the scope",
      "type": "string"
    }
  },
  "required": [
    "calendar_id",
    "role",
    "scope_type"
  ],
  "type": "object"
}
```

#### `calendar.acl.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/calendar/acl/delete`
- **Google path:** `/calendar/v3/calendars/{calendarId}/acl/{ruleId}`
- **Enabled by default:** `No`

Delete an ACL rule

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "rule_id": {
      "description": "The ACL rule ID to delete",
      "type": "string"
    }
  },
  "required": [
    "calendar_id",
    "rule_id"
  ],
  "type": "object"
}
```

#### `calendar.calendarlist.get`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/calendarlist/get`
- **Google path:** `/calendar/v3/users/me/calendarList/{calendarId}`
- **Enabled by default:** `Yes`

Get a calendar list entry

```json
{
  "properties": {
    "calendar_id": {
      "description": "Calendar identifier",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.calendarlist.insert`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/calendarlist/insert`
- **Google path:** `/calendar/v3/users/me/calendarList`
- **Enabled by default:** `No`

Add a calendar to the user's list

```json
{
  "properties": {
    "calendar_id": {
      "description": "ID of the calendar to add",
      "type": "string"
    },
    "summary_override": {
      "description": "Custom name for the calendar",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.calendarlist.update`

- **Method:** `PUT`
- **URL:** `/api/v1/calendar/calendarlist/update`
- **Google path:** `/calendar/v3/users/me/calendarList/{calendarId}`
- **Enabled by default:** `No`

Update a calendar list entry

```json
{
  "properties": {
    "calendar_id": {
      "description": "Calendar identifier",
      "type": "string"
    },
    "hidden": {
      "description": "Whether the calendar is hidden from the list",
      "type": "boolean"
    },
    "selected": {
      "description": "Whether the calendar is selected",
      "type": "boolean"
    },
    "summary_override": {
      "description": "New custom name",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.calendarlist.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/calendar/calendarlist/delete`
- **Google path:** `/calendar/v3/users/me/calendarList/{calendarId}`
- **Enabled by default:** `No`

Remove a calendar from the user's list

```json
{
  "properties": {
    "calendar_id": {
      "description": "Calendar identifier to remove",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.calendars.update`

- **Method:** `PUT`
- **URL:** `/api/v1/calendar/calendars/update`
- **Google path:** `/calendar/v3/calendars/{calendarId}`
- **Enabled by default:** `No`

Update calendar metadata

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    },
    "description": {
      "description": "New description for the calendar",
      "type": "string"
    },
    "summary": {
      "description": "New title for the calendar",
      "type": "string"
    },
    "timezone": {
      "description": "New time zone (e.g., 'America/Chicago')",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.calendars.clear`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/calendars/clear`
- **Google path:** `/calendar/v3/calendars/{calendarId}/clear`
- **Enabled by default:** `No`

Clear all events from a calendar

```json
{
  "properties": {
    "calendar_id": {
      "default": "primary",
      "description": "Calendar identifier",
      "type": "string"
    }
  },
  "required": [
    "calendar_id"
  ],
  "type": "object"
}
```

#### `calendar.colors.get`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/colors/get`
- **Google path:** `/calendar/v3/colors`
- **Enabled by default:** `Yes`

Get color definitions for calendars and events

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.events.move`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/events/move`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/{eventId}/move`
- **Enabled by default:** `No`

Move an event to another calendar

```json
{
  "properties": {
    "calendar_id": {
      "description": "Source calendar identifier",
      "type": "string"
    },
    "destination": {
      "description": "Destination calendar identifier",
      "type": "string"
    },
    "event_id": {
      "description": "Event identifier",
      "type": "string"
    }
  },
  "required": [
    "calendar_id",
    "event_id",
    "destination"
  ],
  "type": "object"
}
```

#### `calendar.settings.list`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/settings/list`
- **Google path:** `/calendar/v3/users/me/settings`
- **Enabled by default:** `Yes`

List user settings

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.settings.get`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/settings/get`
- **Google path:** `/calendar/v3/users/me/settings/{setting}`
- **Enabled by default:** `Yes`

Get a specific user setting

```json
{
  "properties": {
    "setting": {
      "description": "The setting ID (e.g., 'timezone')",
      "type": "string"
    }
  },
  "required": [
    "setting"
  ],
  "type": "object"
}
```

#### `calendar.acl.patch`

- **Method:** `PATCH`
- **URL:** `/api/v1/calendar/acl/patch`
- **Google path:** `/calendar/v3/calendars/{calendarId}/acl/{ruleId}`
- **Query params:** `sendNotifications`
- **Enabled by default:** `No`

Updates an access control rule. This method supports patch semantics.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.acl.update`

- **Method:** `PUT`
- **URL:** `/api/v1/calendar/acl/update`
- **Google path:** `/calendar/v3/calendars/{calendarId}/acl/{ruleId}`
- **Query params:** `sendNotifications`
- **Enabled by default:** `No`

Updates an access control rule.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.acl.watch`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/acl/watch`
- **Google path:** `/calendar/v3/calendars/{calendarId}/acl/watch`
- **Query params:** `maxResults`, `pageToken`, `showDeleted`, `syncToken`
- **Enabled by default:** `No`

Watch for changes to ACL resources.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.calendar_list.patch`

- **Method:** `PATCH`
- **URL:** `/api/v1/calendar/calendar_list/patch`
- **Google path:** `/calendar/v3/users/me/calendarList/{calendarId}`
- **Query params:** `colorRgbFormat`
- **Enabled by default:** `No`

Updates an existing calendar on the user's calendar list. This method supports patch semantics.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.calendar_list.watch`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/calendar_list/watch`
- **Google path:** `/calendar/v3/users/me/calendarList/watch`
- **Query params:** `maxResults`, `minAccessRole`, `pageToken`, `showDeleted`, `showHidden`, `syncToken`
- **Enabled by default:** `No`

Watch for changes to CalendarList resources.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.calendars.patch`

- **Method:** `PATCH`
- **URL:** `/api/v1/calendar/calendars/patch`
- **Google path:** `/calendar/v3/calendars/{calendarId}`
- **Enabled by default:** `No`

Updates metadata for a calendar. This method supports patch semantics.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.channels.stop`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/channels/stop`
- **Google path:** `/calendar/v3/channels/stop`
- **Enabled by default:** `No`

Stop watching resources through this channel

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.events.import`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/events/import`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/import`
- **Query params:** `conferenceDataVersion`, `supportsAttachments`
- **Enabled by default:** `No`

Imports an event. This operation is used to add a private copy of an existing event to a calendar.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.events.instances`

- **Method:** `GET`
- **URL:** `/api/v1/calendar/events/instances`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/{eventId}/instances`
- **Query params:** `alwaysIncludeEmail`, `maxAttendees`, `maxResults`, `originalStart`, `pageToken`, `showDeleted`, `timeMax`, `timeMin`, `timeZone`
- **Enabled by default:** `Yes`

Returns instances of the specified recurring event.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.events.patch`

- **Method:** `PATCH`
- **URL:** `/api/v1/calendar/events/patch`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/{eventId}`
- **Query params:** `alwaysIncludeEmail`, `conferenceDataVersion`, `maxAttendees`, `sendNotifications`, `sendUpdates`, `supportsAttachments`
- **Enabled by default:** `No`

Updates an event. This method supports patch semantics.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.events.watch`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/events/watch`
- **Google path:** `/calendar/v3/calendars/{calendarId}/events/watch`
- **Query params:** `alwaysIncludeEmail`, `eventTypes`, `iCalUID`, `maxAttendees`, `maxResults`, `orderBy`, `pageToken`, `privateExtendedProperty`, `q`, `sharedExtendedProperty`, `showDeleted`, `showHiddenInvitations`, `singleEvents`, `syncToken`, `timeMax`, `timeMin`, `timeZone`, `updatedMin`
- **Enabled by default:** `No`

Watch for changes to Events resources.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `calendar.settings.watch`

- **Method:** `POST`
- **URL:** `/api/v1/calendar/settings/watch`
- **Google path:** `/calendar/v3/users/me/settings/watch`
- **Query params:** `maxResults`, `pageToken`, `syncToken`
- **Enabled by default:** `No`

Watch for changes to Settings resources.

```json
{
  "properties": {},
  "type": "object"
}
```

## Module: `drive` — Google Drive

Browse, search, and read files in Google Drive, including Sheets, Docs, and Slides content

### Required OAuth scopes

- `https://www.googleapis.com/auth/drive`
- `https://www.googleapis.com/auth/spreadsheets`
- `https://www.googleapis.com/auth/documents`
- `https://www.googleapis.com/auth/presentations`

### Routes (83)

#### `drive.files.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/list`
- **Google path:** `/drive/v3/files`
- **Query params:** `fields`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_results": 50}`

List and search for files in Drive

```json
{
  "properties": {
    "fields": {
      "default": "files(id,name,mimeType,modifiedTime,size,owners,shared,parents),nextPageToken",
      "description": "Fields to include in the response (e.g., 'files(id,name,mimeType)')",
      "type": "string"
    },
    "order_by": {
      "default": "modifiedTime",
      "description": "Sort field (e.g., 'modifiedTime')",
      "type": "string"
    },
    "page_size": {
      "default": 20,
      "description": "Number of files to return (max 100)",
      "type": "integer"
    },
    "q": {
      "description": "Drive query string (e.g., \"name contains 'report'\"). See: https://developers.google.com/drive/api/v3/search-files",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `drive.files.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/get`
- **Google path:** `/drive/v3/files/{fileId}`
- **Query params:** `fields`
- **Enabled by default:** `Yes`

Get file metadata by ID

```json
{
  "properties": {
    "fields": {
      "default": "id,name,mimeType,modifiedTime,size,owners,shared,parents",
      "description": "Fields to include in the response",
      "type": "string"
    },
    "file_id": {
      "description": "The ID of the file to retrieve",
      "type": "string"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.files.export`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/export`
- **Google path:** `/drive/v3/files/{fileId}/export`
- **Binary response:** `True`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_inline_size_mb": 1}`

Export a Google Workspace document to a given MIME type

```json
{
  "properties": {
    "file_id": {
      "description": "The ID of the file to export",
      "type": "string"
    },
    "mime_type": {
      "default": "application/pdf",
      "description": "Target MIME type (e.g., 'application/pdf')",
      "type": "string"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.files.download`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/download`
- **Google path:** `/drive/v3/files/{fileId}`
- **Binary response:** `True`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_inline_size_mb": 1}`

Download a file's binary content from Google Drive

```json
{
  "properties": {
    "acknowledge_abuse": {
      "default": false,
      "description": "Acknowledge potential abuse if flagged by Google",
      "type": "boolean"
    },
    "file_id": {
      "description": "The ID of the file to download",
      "type": "string"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.files.list_shared`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/list_shared`
- **Google path:** `/drive/v3/files`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_results": 50, "query_filter": "sharedWithMe=true"}`

List files shared with the user

```json
{
  "properties": {
    "page_size": {
      "default": 20,
      "description": "Number of files to return",
      "type": "integer"
    }
  },
  "type": "object"
}
```

#### `drive.files.copy`

- **Method:** `POST`
- **URL:** `/api/v1/drive/files/copy`
- **Google path:** `/drive/v3/files/{fileId}/copy`
- **Enabled by default:** `No`

Copy a file (requires drive scope)

```json
{
  "properties": {
    "file_id": {
      "description": "ID of the file to copy",
      "type": "string"
    },
    "name": {
      "description": "Name for the copy",
      "type": "string"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.files.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/files/create`
- **Google path:** `/drive/v3/files`
- **Enabled by default:** `No`
- **Default policy:** `{"max_file_size_mb": 25}`

Upload or create a new file in Drive

```json
{
  "properties": {
    "mime_type": {
      "description": "MIME type of the file",
      "type": "string"
    },
    "name": {
      "description": "Name for the new file",
      "type": "string"
    },
    "parents": {
      "description": "Parent folder IDs to add the file to",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "shortcut_target_id": {
      "description": "File ID the shortcut points to (only used when mime_type is shortcut)",
      "type": "string"
    },
    "shortcut_target_mime_type": {
      "description": "MIME type of the target file (only used when mime_type is shortcut)",
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "type": "object"
}
```

#### `drive.files.upload`

- **Method:** `POST`
- **URL:** `/api/v1/drive/files/upload`
- **Google path:** `/upload/drive/v3/files`
- **Query params:** `uploadType`
- **Multipart upload:** `True`
- **Enabled by default:** `No`
- **Default policy:** `{"max_file_size_mb": 25}`

Upload a new file with content to Google Drive via multipart

```json
{
  "properties": {
    "base64_content": {
      "description": "Base64-encoded file content",
      "type": "string"
    },
    "description": {
      "description": "File description",
      "type": "string"
    },
    "mime_type": {
      "description": "MIME type of the file. Guessed from name if omitted.",
      "type": "string"
    },
    "name": {
      "description": "Name for the new file",
      "type": "string"
    },
    "parents": {
      "description": "Parent folder IDs to add the file to",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "name",
    "base64_content"
  ],
  "type": "object"
}
```

#### `drive.files.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/drive/files/update`
- **Google path:** `/drive/v3/files/{fileId}`
- **Query params:** `addParents`, `removeParents`
- **Enabled by default:** `Yes`

Update file metadata or content

```json
{
  "properties": {
    "add_parents": {
      "description": "Comma-separated parent folder IDs to add (for moving files)",
      "type": "string"
    },
    "description": {
      "description": "New description for the file",
      "type": "string"
    },
    "file_id": {
      "description": "The ID of the file to update",
      "type": "string"
    },
    "name": {
      "description": "New name for the file",
      "type": "string"
    },
    "remove_parents": {
      "description": "Comma-separated parent folder IDs to remove (for moving files)",
      "type": "string"
    },
    "starred": {
      "description": "Whether the file is starred",
      "type": "boolean"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.files.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/files/delete`
- **Google path:** `/drive/v3/files/{fileId}`
- **Enabled by default:** `No`

Permanently delete a file (cannot be undone)

```json
{
  "properties": {
    "file_id": {
      "description": "The ID of the file to permanently delete",
      "type": "string"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.files.trash`

- **Method:** `POST`
- **URL:** `/api/v1/drive/files/trash`
- **Google path:** `/drive/v3/files/{fileId}`
- **Enabled by default:** `No`

Move a file to trash (recoverable)

```json
{
  "properties": {
    "file_id": {
      "description": "The ID of the file to trash",
      "type": "string"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.permissions.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/permissions/list`
- **Google path:** `/drive/v3/files/{fileId}/permissions`
- **Enabled by default:** `Yes`

See who a file is shared with

```json
{
  "properties": {
    "file_id": {
      "description": "The ID of the file to check permissions for",
      "type": "string"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.permissions.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/permissions/get`
- **Google path:** `/drive/v3/files/{fileId}/permissions/{permissionId}`
- **Enabled by default:** `Yes`

Get details of a specific permission on a file

```json
{
  "properties": {
    "file_id": {
      "description": "The ID of the file",
      "type": "string"
    },
    "permission_id": {
      "description": "The ID of the permission to retrieve",
      "type": "string"
    }
  },
  "required": [
    "file_id",
    "permission_id"
  ],
  "type": "object"
}
```

#### `drive.permissions.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/permissions/create`
- **Google path:** `/drive/v3/files/{fileId}/permissions`
- **Enabled by default:** `No`
- **Default policy:** `{"max_recipients": 5}`

Share a file with someone — add a new permission

```json
{
  "properties": {
    "email_address": {
      "description": "Email address of the person to share with",
      "type": "string"
    },
    "file_id": {
      "description": "The ID of the file to share",
      "type": "string"
    },
    "role": {
      "default": "reader",
      "description": "Role to grant: reader, writer, organizer, or owner",
      "type": "string"
    },
    "type": {
      "default": "user",
      "description": "Permission type: user, group, or anyone",
      "type": "string"
    }
  },
  "required": [
    "file_id",
    "email_address"
  ],
  "type": "object"
}
```

#### `drive.permissions.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/permissions/delete`
- **Google path:** `/drive/v3/files/{fileId}/permissions/{permissionId}`
- **Enabled by default:** `No`

Remove someone's access to a file

```json
{
  "properties": {
    "file_id": {
      "description": "The ID of the file",
      "type": "string"
    },
    "permission_id": {
      "description": "The ID of the permission to remove",
      "type": "string"
    }
  },
  "required": [
    "file_id",
    "permission_id"
  ],
  "type": "object"
}
```

#### `drive.about.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/about/get`
- **Google path:** `/drive/v3/about`
- **Enabled by default:** `Yes`

Get user Drive storage quota and usage info

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.changes.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/changes/list`
- **Google path:** `/drive/v3/changes`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_results": 100}`

List changes to files (incremental sync)

```json
{
  "properties": {
    "page_size": {
      "default": 100,
      "description": "Number of changes to return",
      "type": "integer"
    },
    "page_token": {
      "description": "Token for continuing a previous list request",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `drive.changes.get_start_page_token`

- **Method:** `GET`
- **URL:** `/api/v1/drive/changes/get_start_page_token`
- **Google path:** `/drive/v3/changes/startPageToken`
- **Enabled by default:** `Yes`

Get start page token for change tracking

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.comments.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/comments/list`
- **Google path:** `/drive/v3/files/{fileId}/comments`
- **Enabled by default:** `Yes`

List comments on a file

```json
{
  "properties": {
    "file_id": {
      "description": "ID of the file",
      "type": "string"
    },
    "page_size": {
      "default": 20,
      "description": "Number of comments to return",
      "type": "integer"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.comments.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/comments/get`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}`
- **Enabled by default:** `Yes`

Get a comment by ID

```json
{
  "properties": {
    "comment_id": {
      "description": "ID of the comment",
      "type": "string"
    },
    "file_id": {
      "description": "ID of the file",
      "type": "string"
    }
  },
  "required": [
    "file_id",
    "comment_id"
  ],
  "type": "object"
}
```

#### `drive.comments.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/comments/create`
- **Google path:** `/drive/v3/files/{fileId}/comments`
- **Enabled by default:** `No`

Create a comment on a file

```json
{
  "properties": {
    "content": {
      "description": "Comment text",
      "type": "string"
    },
    "file_id": {
      "description": "ID of the file",
      "type": "string"
    }
  },
  "required": [
    "file_id",
    "content"
  ],
  "type": "object"
}
```

#### `drive.drives.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/drives/list`
- **Google path:** `/drive/v3/drives`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_results": 50}`

List shared drives

```json
{
  "properties": {
    "page_size": {
      "default": 20,
      "description": "Number of drives to return",
      "type": "integer"
    }
  },
  "type": "object"
}
```

#### `drive.drives.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/drives/get`
- **Google path:** `/drive/v3/drives/{driveId}`
- **Enabled by default:** `Yes`

Get shared drive metadata

```json
{
  "properties": {
    "drive_id": {
      "description": "ID of the shared drive",
      "type": "string"
    }
  },
  "required": [
    "drive_id"
  ],
  "type": "object"
}
```

#### `drive.drives.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/drives/create`
- **Google path:** `/drive/v3/drives`
- **Enabled by default:** `No`

Create a shared drive

```json
{
  "properties": {
    "name": {
      "description": "Name of the new shared drive",
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "type": "object"
}
```

#### `drive.files.empty_trash`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/files/empty_trash`
- **Google path:** `/drive/v3/files/trash`
- **Enabled by default:** `No`

Permanently delete all trashed files

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.files.generate_ids`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/generate_ids`
- **Google path:** `/drive/v3/files/generateIds`
- **Enabled by default:** `Yes`

Generate file IDs for future uploads

```json
{
  "properties": {
    "count": {
      "default": 10,
      "description": "Number of IDs to generate",
      "type": "integer"
    }
  },
  "type": "object"
}
```

#### `drive.permissions.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/drive/permissions/update`
- **Google path:** `/drive/v3/files/{fileId}/permissions/{permissionId}`
- **Enabled by default:** `No`

Update a permission on a file

```json
{
  "properties": {
    "file_id": {
      "description": "ID of the file",
      "type": "string"
    },
    "permission_id": {
      "description": "ID of the permission to update",
      "type": "string"
    },
    "role": {
      "description": "New role: reader, writer, organizer, or owner",
      "type": "string"
    }
  },
  "required": [
    "file_id",
    "permission_id"
  ],
  "type": "object"
}
```

#### `drive.revisions.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/revisions/list`
- **Google path:** `/drive/v3/files/{fileId}/revisions`
- **Enabled by default:** `Yes`

List file revisions

```json
{
  "properties": {
    "file_id": {
      "description": "ID of the file",
      "type": "string"
    },
    "page_size": {
      "default": 20,
      "description": "Number of revisions to return",
      "type": "integer"
    }
  },
  "required": [
    "file_id"
  ],
  "type": "object"
}
```

#### `drive.revisions.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/revisions/get`
- **Google path:** `/drive/v3/files/{fileId}/revisions/{revisionId}`
- **Enabled by default:** `Yes`

Get a specific file revision

```json
{
  "properties": {
    "file_id": {
      "description": "ID of the file",
      "type": "string"
    },
    "revision_id": {
      "description": "ID of the revision",
      "type": "string"
    }
  },
  "required": [
    "file_id",
    "revision_id"
  ],
  "type": "object"
}
```

#### `drive.accessproposals.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/accessproposals/get`
- **Google path:** `/drive/v3/files/{fileId}/accessproposals/{proposalId}`
- **Enabled by default:** `Yes`

Retrieves an access proposal by ID.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.accessproposals.resolve`

- **Method:** `POST`
- **URL:** `/api/v1/drive/accessproposals/resolve`
- **Google path:** `/drive/v3/files/{fileId}/accessproposals/{proposalId}:resolve`
- **Enabled by default:** `No`

Approves or denies an access proposal.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.accessproposals.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/accessproposals/list`
- **Google path:** `/drive/v3/files/{fileId}/accessproposals`
- **Query params:** `pageSize`, `pageToken`
- **Enabled by default:** `Yes`

List the access proposals on a file.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/approvals/get`
- **Google path:** `/drive/v3/files/{fileId}/approvals/{approvalId}`
- **Enabled by default:** `Yes`

Gets an approval by ID.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/approvals/list`
- **Google path:** `/drive/v3/files/{fileId}/approvals`
- **Query params:** `pageSize`, `pageToken`
- **Enabled by default:** `Yes`

Lists the approvals on a file.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.start`

- **Method:** `POST`
- **URL:** `/api/v1/drive/approvals/start`
- **Google path:** `/drive/v3/files/{fileId}/approvals:start`
- **Enabled by default:** `No`

Starts an approval on a file.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.approve`

- **Method:** `POST`
- **URL:** `/api/v1/drive/approvals/approve`
- **Google path:** `/drive/v3/files/{fileId}/approvals/{approvalId}:approve`
- **Enabled by default:** `No`

Approves an approval.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.decline`

- **Method:** `POST`
- **URL:** `/api/v1/drive/approvals/decline`
- **Google path:** `/drive/v3/files/{fileId}/approvals/{approvalId}:decline`
- **Enabled by default:** `No`

Declines an approval.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.cancel`

- **Method:** `POST`
- **URL:** `/api/v1/drive/approvals/cancel`
- **Google path:** `/drive/v3/files/{fileId}/approvals/{approvalId}:cancel`
- **Enabled by default:** `No`

Cancels an approval.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.comment`

- **Method:** `POST`
- **URL:** `/api/v1/drive/approvals/comment`
- **Google path:** `/drive/v3/files/{fileId}/approvals/{approvalId}:comment`
- **Enabled by default:** `No`

Comments on an approval.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.approvals.reassign`

- **Method:** `POST`
- **URL:** `/api/v1/drive/approvals/reassign`
- **Google path:** `/drive/v3/files/{fileId}/approvals/{approvalId}:reassign`
- **Enabled by default:** `No`

Reassigns the reviewers on an approval.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.apps.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/apps/get`
- **Google path:** `/drive/v3/apps/{appId}`
- **Enabled by default:** `Yes`

Gets a specific app.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.apps.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/apps/list`
- **Google path:** `/drive/v3/apps`
- **Query params:** `appFilterExtensions`, `appFilterMimeTypes`, `languageCode`
- **Enabled by default:** `Yes`

Lists a user's installed apps.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.changes.watch`

- **Method:** `POST`
- **URL:** `/api/v1/drive/changes/watch`
- **Google path:** `/drive/v3/changes/watch`
- **Query params:** `driveId`, `includeCorpusRemovals`, `includeItemsFromAllDrives`, `includeLabels`, `includePermissionsForView`, `includeRemoved`, `includeTeamDriveItems`, `pageSize`, `pageToken`, `restrictToMyDrive`, `spaces`, `supportsAllDrives`, `supportsTeamDrives`, `teamDriveId`
- **Enabled by default:** `No`

Subscribes to changes for a user.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.channels.stop`

- **Method:** `POST`
- **URL:** `/api/v1/drive/channels/stop`
- **Google path:** `/drive/v3/channels/stop`
- **Enabled by default:** `No`

Stops watching resources through this channel.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.comments.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/comments/delete`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}`
- **Enabled by default:** `No`

Deletes a comment.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.comments.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/drive/comments/update`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}`
- **Enabled by default:** `No`

Updates a comment with patch semantics.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.drives.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/drives/delete`
- **Google path:** `/drive/v3/drives/{driveId}`
- **Query params:** `allowItemDeletion`, `useDomainAdminAccess`
- **Enabled by default:** `No`

Permanently deletes a shared drive.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.drives.hide`

- **Method:** `POST`
- **URL:** `/api/v1/drive/drives/hide`
- **Google path:** `/drive/v3/drives/{driveId}/hide`
- **Enabled by default:** `No`

Hides a shared drive from the default view.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.drives.unhide`

- **Method:** `POST`
- **URL:** `/api/v1/drive/drives/unhide`
- **Google path:** `/drive/v3/drives/{driveId}/unhide`
- **Enabled by default:** `No`

Restores a shared drive to the default view.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.drives.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/drive/drives/update`
- **Google path:** `/drive/v3/drives/{driveId}`
- **Query params:** `useDomainAdminAccess`
- **Enabled by default:** `No`

Updates the metadata for a shared drive.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.files.generate_cse_token`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/generate_cse_token`
- **Google path:** `/drive/v3/files/generateCseToken`
- **Query params:** `fileId`, `parent`
- **Enabled by default:** `Yes`

Generates a CSE token for creating or updating CSE files.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.files.list_labels`

- **Method:** `GET`
- **URL:** `/api/v1/drive/files/list_labels`
- **Google path:** `/drive/v3/files/{fileId}/listLabels`
- **Query params:** `maxResults`, `pageToken`
- **Enabled by default:** `Yes`

Lists the labels on a file.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.files.modify_labels`

- **Method:** `POST`
- **URL:** `/api/v1/drive/files/modify_labels`
- **Google path:** `/drive/v3/files/{fileId}/modifyLabels`
- **Enabled by default:** `No`

Modifies the set of labels applied to a file.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.files.watch`

- **Method:** `POST`
- **URL:** `/api/v1/drive/files/watch`
- **Google path:** `/drive/v3/files/{fileId}/watch`
- **Query params:** `acknowledgeAbuse`, `includeLabels`, `includePermissionsForView`, `supportsAllDrives`, `supportsTeamDrives`
- **Enabled by default:** `No`

Subscribes to changes to a file.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.operations.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/operations/get`
- **Google path:** `/drive/v3/operations/{name}`
- **Enabled by default:** `Yes`

Gets the latest state of a long-running operation.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.replies.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/replies/create`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}/replies`
- **Enabled by default:** `No`

Creates a reply to a comment.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.replies.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/replies/delete`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}/replies/{replyId}`
- **Enabled by default:** `No`

Deletes a reply.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.replies.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/replies/get`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}/replies/{replyId}`
- **Query params:** `includeDeleted`
- **Enabled by default:** `Yes`

Gets a reply by ID.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.replies.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/replies/list`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}/replies`
- **Query params:** `includeDeleted`, `pageSize`, `pageToken`
- **Enabled by default:** `Yes`

Lists a comment's replies.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.replies.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/drive/replies/update`
- **Google path:** `/drive/v3/files/{fileId}/comments/{commentId}/replies/{replyId}`
- **Enabled by default:** `No`

Updates a reply with patch semantics.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.revisions.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/revisions/delete`
- **Google path:** `/drive/v3/files/{fileId}/revisions/{revisionId}`
- **Enabled by default:** `No`

Permanently deletes a file version.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.revisions.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/drive/revisions/update`
- **Google path:** `/drive/v3/files/{fileId}/revisions/{revisionId}`
- **Enabled by default:** `No`

Updates a revision with patch semantics.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.teamdrives.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/teamdrives/create`
- **Google path:** `/drive/v3/teamdrives`
- **Query params:** `requestId`
- **Enabled by default:** `No`

Deprecated: Use drives.create instead.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.teamdrives.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/drive/teamdrives/delete`
- **Google path:** `/drive/v3/teamdrives/{teamDriveId}`
- **Enabled by default:** `No`

Deprecated: Use drives.delete instead.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.teamdrives.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/teamdrives/get`
- **Google path:** `/drive/v3/teamdrives/{teamDriveId}`
- **Query params:** `useDomainAdminAccess`
- **Enabled by default:** `Yes`

Deprecated: Use drives.get instead.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.teamdrives.list`

- **Method:** `GET`
- **URL:** `/api/v1/drive/teamdrives/list`
- **Google path:** `/drive/v3/teamdrives`
- **Query params:** `pageSize`, `pageToken`, `q`, `useDomainAdminAccess`
- **Enabled by default:** `Yes`

Deprecated: Use drives.list instead.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.teamdrives.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/drive/teamdrives/update`
- **Google path:** `/drive/v3/teamdrives/{teamDriveId}`
- **Query params:** `useDomainAdminAccess`
- **Enabled by default:** `No`

Deprecated: Use drives.update instead.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `drive.sheets.spreadsheets.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/sheets/spreadsheets/get`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}`
- **Base URL:** `https://sheets.googleapis.com`
- **Query params:** `fields`
- **Enabled by default:** `Yes`

Get spreadsheet metadata (sheets, named ranges, properties)

```json
{
  "properties": {
    "fields": {
      "description": "Fields to include in the response (partial response)",
      "type": "string"
    },
    "spreadsheet_id": {
      "description": "The ID of the spreadsheet to retrieve",
      "type": "string"
    }
  },
  "required": [
    "spreadsheet_id"
  ],
  "type": "object"
}
```

#### `drive.sheets.values.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/sheets/values/get`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}/values/{range}`
- **Base URL:** `https://sheets.googleapis.com`
- **Query params:** `value_render_option`, `date_time_render_option`, `major_dimension`
- **Enabled by default:** `Yes`

Read a single range of cell values from a spreadsheet

```json
{
  "properties": {
    "date_time_render_option": {
      "description": "SERIAL_NUMBER or FORMATTED_STRING",
      "type": "string"
    },
    "major_dimension": {
      "description": "ROWS or COLUMNS",
      "type": "string"
    },
    "range": {
      "description": "A1 or R1C1 notation of the range (e.g., 'Sheet1!A1:C10')",
      "type": "string"
    },
    "spreadsheet_id": {
      "description": "The ID of the spreadsheet",
      "type": "string"
    },
    "value_render_option": {
      "default": "FORMATTED_VALUE",
      "description": "FORMATTED_VALUE, UNFORMATTED_VALUE, or FORMULA",
      "type": "string"
    }
  },
  "required": [
    "spreadsheet_id",
    "range"
  ],
  "type": "object"
}
```

#### `drive.sheets.values.batch_get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/sheets/values/batch_get`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}/values:batchGet`
- **Base URL:** `https://sheets.googleapis.com`
- **Query params:** `ranges`, `value_render_option`, `date_time_render_option`, `major_dimension`
- **Enabled by default:** `Yes`

Read multiple ranges of cell values in one request

```json
{
  "properties": {
    "date_time_render_option": {
      "type": "string"
    },
    "major_dimension": {
      "description": "ROWS or COLUMNS",
      "type": "string"
    },
    "ranges": {
      "description": "A1/R1C1 ranges to retrieve (e.g., ['Sheet1!A1:B2'])",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "spreadsheet_id": {
      "description": "The ID of the spreadsheet",
      "type": "string"
    },
    "value_render_option": {
      "description": "FORMATTED_VALUE, UNFORMATTED_VALUE, or FORMULA",
      "type": "string"
    }
  },
  "required": [
    "spreadsheet_id"
  ],
  "type": "object"
}
```

#### `drive.sheets.values.update`

- **Method:** `PUT`
- **URL:** `/api/v1/drive/sheets/values/update`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}/values/{range}`
- **Base URL:** `https://sheets.googleapis.com`
- **Query params:** `value_input_option`
- **Enabled by default:** `No`

Write values to a range of cells in a spreadsheet

```json
{
  "properties": {
    "range": {
      "description": "A1/R1C1 range to write",
      "type": "string"
    },
    "spreadsheet_id": {
      "type": "string"
    },
    "value_input_option": {
      "default": "RAW",
      "description": "RAW or USER_ENTERED",
      "type": "string"
    },
    "values": {
      "description": "2D array of values (e.g., [['A', 1], ['B', 2]])",
      "items": {
        "items": {},
        "type": "array"
      },
      "type": "array"
    }
  },
  "required": [
    "spreadsheet_id",
    "range",
    "values"
  ],
  "type": "object"
}
```

#### `drive.sheets.values.append`

- **Method:** `POST`
- **URL:** `/api/v1/drive/sheets/values/append`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}/values/{range}:append`
- **Base URL:** `https://sheets.googleapis.com`
- **Query params:** `value_input_option`, `insert_data_option`
- **Enabled by default:** `No`

Append values after the last row of data in a range

```json
{
  "properties": {
    "insert_data_option": {
      "default": "OVERWRITE",
      "description": "OVERWRITE or INSERT_ROWS",
      "type": "string"
    },
    "range": {
      "description": "A1 notation of the table to search (e.g., 'Sheet1!A1:B')",
      "type": "string"
    },
    "spreadsheet_id": {
      "type": "string"
    },
    "value_input_option": {
      "default": "RAW",
      "description": "RAW or USER_ENTERED",
      "type": "string"
    },
    "values": {
      "description": "2D array of values to append",
      "items": {
        "items": {},
        "type": "array"
      },
      "type": "array"
    }
  },
  "required": [
    "spreadsheet_id",
    "range",
    "values"
  ],
  "type": "object"
}
```

#### `drive.sheets.values.clear`

- **Method:** `POST`
- **URL:** `/api/v1/drive/sheets/values/clear`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}/values/{range}:clear`
- **Base URL:** `https://sheets.googleapis.com`
- **Enabled by default:** `No`

Clear values from a range of cells

```json
{
  "properties": {
    "range": {
      "description": "A1/R1C1 range to clear",
      "type": "string"
    },
    "spreadsheet_id": {
      "type": "string"
    }
  },
  "required": [
    "spreadsheet_id",
    "range"
  ],
  "type": "object"
}
```

#### `drive.sheets.values.batch_update`

- **Method:** `POST`
- **URL:** `/api/v1/drive/sheets/values/batch_update`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}/values:batchUpdate`
- **Base URL:** `https://sheets.googleapis.com`
- **Query params:** `value_input_option`
- **Enabled by default:** `No`

Update multiple ranges of cell values in a single request

```json
{
  "properties": {
    "data": {
      "description": "One entry per range to write",
      "items": {
        "properties": {
          "range": {
            "type": "string"
          },
          "values": {
            "items": {
              "items": {},
              "type": "array"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "type": "array"
    },
    "spreadsheet_id": {
      "type": "string"
    },
    "value_input_option": {
      "default": "RAW",
      "description": "RAW or USER_ENTERED",
      "type": "string"
    }
  },
  "required": [
    "spreadsheet_id",
    "data"
  ],
  "type": "object"
}
```

#### `drive.sheets.spreadsheets.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/sheets/spreadsheets/create`
- **Google path:** `/v4/spreadsheets`
- **Base URL:** `https://sheets.googleapis.com`
- **Enabled by default:** `No`

Create a new spreadsheet (optional title and sheets)

```json
{
  "properties": {
    "sheet_titles": {
      "description": "Names of sheets to create within the spreadsheet",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "title": {
      "description": "Title of the new spreadsheet",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `drive.sheets.spreadsheets.batch_update`

- **Method:** `POST`
- **URL:** `/api/v1/drive/sheets/spreadsheets/batch_update`
- **Google path:** `/v4/spreadsheets/{spreadsheetId}:batchUpdate`
- **Base URL:** `https://sheets.googleapis.com`
- **Enabled by default:** `No`

Apply one or more updates to a spreadsheet (formatting, formulas, charts, etc.)

```json
{
  "properties": {
    "requests": {
      "description": "List of update request objects (see Sheets API batchUpdate reference)",
      "items": {
        "type": "object"
      },
      "type": "array"
    },
    "spreadsheet_id": {
      "type": "string"
    }
  },
  "required": [
    "spreadsheet_id",
    "requests"
  ],
  "type": "object"
}
```

#### `drive.docs.documents.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/docs/documents/get`
- **Google path:** `/v1/documents/{documentId}`
- **Base URL:** `https://docs.googleapis.com`
- **Query params:** `suggestions_view_mode`
- **Enabled by default:** `Yes`

Get the full content and structure of a Google Doc

```json
{
  "properties": {
    "document_id": {
      "type": "string"
    },
    "suggestions_view_mode": {
      "description": "SUGGESTIONS_INLINE, PREVIEW_SUGGESTIONS_ACCEPTED, or PREVIEW_WITHOUT_SUGGESTIONS",
      "type": "string"
    }
  },
  "required": [
    "document_id"
  ],
  "type": "object"
}
```

#### `drive.docs.documents.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/docs/documents/create`
- **Google path:** `/v1/documents`
- **Base URL:** `https://docs.googleapis.com`
- **Enabled by default:** `No`

Create a new Google Doc with an optional title

```json
{
  "properties": {
    "title": {
      "description": "Title for the new document",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `drive.docs.documents.batch_update`

- **Method:** `POST`
- **URL:** `/api/v1/drive/docs/documents/batch_update`
- **Google path:** `/v1/documents/{documentId}:batchUpdate`
- **Base URL:** `https://docs.googleapis.com`
- **Enabled by default:** `No`

Apply one or more updates to a Google Doc (insert text, delete, style, etc.)

```json
{
  "properties": {
    "document_id": {
      "type": "string"
    },
    "requests": {
      "description": "Update requests (InsertTextRequest, DeleteContentRangeRequest, etc.)",
      "items": {
        "type": "object"
      },
      "type": "array"
    },
    "write_control": {
      "description": "Optional concurrency control (required_revision_id / target_revision_id)",
      "type": "object"
    }
  },
  "required": [
    "document_id",
    "requests"
  ],
  "type": "object"
}
```

#### `drive.slides.presentations.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/slides/presentations/get`
- **Google path:** `/v1/presentations/{presentationId}`
- **Base URL:** `https://slides.googleapis.com`
- **Enabled by default:** `Yes`

Get the full content and structure of a Google Slides presentation

```json
{
  "properties": {
    "presentation_id": {
      "type": "string"
    }
  },
  "required": [
    "presentation_id"
  ],
  "type": "object"
}
```

#### `drive.slides.presentations.pages.get`

- **Method:** `GET`
- **URL:** `/api/v1/drive/slides/presentations/pages/get`
- **Google path:** `/v1/presentations/{presentationId}/pages/{pageObjectId}`
- **Base URL:** `https://slides.googleapis.com`
- **Enabled by default:** `Yes`

Get a specific page (slide) from a presentation

```json
{
  "properties": {
    "page_object_id": {
      "type": "string"
    },
    "presentation_id": {
      "type": "string"
    }
  },
  "required": [
    "presentation_id",
    "page_object_id"
  ],
  "type": "object"
}
```

#### `drive.slides.presentations.create`

- **Method:** `POST`
- **URL:** `/api/v1/drive/slides/presentations/create`
- **Google path:** `/v1/presentations`
- **Base URL:** `https://slides.googleapis.com`
- **Enabled by default:** `No`

Create a new Google Slides presentation with an optional title

```json
{
  "properties": {
    "title": {
      "description": "Title for the new presentation",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `drive.slides.presentations.batch_update`

- **Method:** `POST`
- **URL:** `/api/v1/drive/slides/presentations/batch_update`
- **Google path:** `/v1/presentations/{presentationId}:batchUpdate`
- **Base URL:** `https://slides.googleapis.com`
- **Enabled by default:** `No`

Apply updates to a presentation (add slides, insert text, update shapes, etc.)

```json
{
  "properties": {
    "presentation_id": {
      "type": "string"
    },
    "requests": {
      "description": "Update requests (CreateSlideRequest, InsertTextRequest, etc.)",
      "items": {
        "type": "object"
      },
      "type": "array"
    }
  },
  "required": [
    "presentation_id",
    "requests"
  ],
  "type": "object"
}
```

## Module: `gmail` — Google Gmail

Read, search, and send email via Gmail

### Required OAuth scopes

- `https://www.googleapis.com/auth/gmail.modify`
- `https://www.googleapis.com/auth/gmail.send`
- `https://www.googleapis.com/auth/gmail.compose`
- `https://www.googleapis.com/auth/gmail.settings.basic`

### Routes (53)

#### `gmail.messages.list`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/messages/list`
- **Google path:** `/gmail/v1/users/me/messages`
- **Enabled by default:** `Yes`
- **Default policy:** `{"allowed_labels": ["INBOX", "SENT", "DRAFT", "UNREAD", "IMPORTANT"], "exclude_labels": ["SPAM", "TRASH"], "max_results": 50}`

List messages in the user's mailbox

```json
{
  "properties": {
    "label_ids": {
      "description": "Label IDs to filter by (e.g., ['INBOX', 'UNREAD'])",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "max_results": {
      "default": 20,
      "description": "Maximum number of messages to return",
      "type": "integer"
    },
    "q": {
      "description": "Gmail search query (e.g., 'from:alice subject:report')",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `gmail.messages.get`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/messages/get`
- **Google path:** `/gmail/v1/users/me/messages/{messageId}`
- **Enabled by default:** `Yes`

Get a specific message by ID

```json
{
  "properties": {
    "format": {
      "default": "full",
      "description": "Format: full, minimal, raw, metadata",
      "type": "string"
    },
    "message_id": {
      "description": "The ID of the message to retrieve",
      "type": "string"
    }
  },
  "required": [
    "message_id"
  ],
  "type": "object"
}
```

#### `gmail.messages.send`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/send`
- **Google path:** `/gmail/v1/users/me/messages/send`
- **Enabled by default:** `No`
- **Default policy:** `{"max_attachment_size_mb": 10, "max_recipients": 5, "require_body": true}`

Send an email message

```json
{
  "properties": {
    "body": {
      "description": "Email body text",
      "type": "string"
    },
    "subject": {
      "description": "Email subject line",
      "type": "string"
    },
    "to": {
      "description": "Recipient email addresses",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "to",
    "subject",
    "body"
  ],
  "type": "object"
}
```

#### `gmail.messages.modify`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/modify`
- **Google path:** `/gmail/v1/users/me/messages/{messageId}/modify`
- **Enabled by default:** `No`

Change labels on a message (archive, mark read, etc.)

```json
{
  "properties": {
    "add_label_ids": {
      "description": "Label IDs to add",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "message_id": {
      "description": "The ID of the message to modify",
      "type": "string"
    },
    "remove_label_ids": {
      "description": "Label IDs to remove",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "message_id"
  ],
  "type": "object"
}
```

#### `gmail.messages.trash`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/trash`
- **Google path:** `/gmail/v1/users/me/messages/{messageId}/trash`
- **Enabled by default:** `No`

Move a message to trash (recoverable)

```json
{
  "properties": {
    "message_id": {
      "description": "The ID of the message to trash",
      "type": "string"
    }
  },
  "required": [
    "message_id"
  ],
  "type": "object"
}
```

#### `gmail.messages.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/gmail/messages/delete`
- **Google path:** `/gmail/v1/users/me/messages/{messageId}`
- **Enabled by default:** `No`

Permanently delete a message (cannot be undone)

```json
{
  "properties": {
    "message_id": {
      "description": "The ID of the message to permanently delete",
      "type": "string"
    }
  },
  "required": [
    "message_id"
  ],
  "type": "object"
}
```

#### `gmail.drafts.list`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/drafts/list`
- **Google path:** `/gmail/v1/users/me/drafts`
- **Enabled by default:** `Yes`
- **Default policy:** `{"max_results": 50}`

List draft messages

```json
{
  "properties": {
    "max_results": {
      "default": 20,
      "description": "Maximum number of drafts to return",
      "type": "integer"
    }
  },
  "type": "object"
}
```

#### `gmail.drafts.get`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/drafts/get`
- **Google path:** `/gmail/v1/users/me/drafts/{draftId}`
- **Enabled by default:** `Yes`

Get a specific draft by ID

```json
{
  "properties": {
    "draft_id": {
      "description": "The ID of the draft to retrieve",
      "type": "string"
    },
    "format": {
      "default": "full",
      "description": "Format: full, minimal, raw, metadata",
      "type": "string"
    }
  },
  "required": [
    "draft_id"
  ],
  "type": "object"
}
```

#### `gmail.drafts.create`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/drafts/create`
- **Google path:** `/gmail/v1/users/me/drafts`
- **Enabled by default:** `No`
- **Default policy:** `{"max_recipients": 5}`

Create a new draft message

```json
{
  "properties": {
    "body": {
      "description": "Email body text",
      "type": "string"
    },
    "subject": {
      "description": "Email subject line",
      "type": "string"
    },
    "to": {
      "description": "Recipient email addresses",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "to",
    "subject",
    "body"
  ],
  "type": "object"
}
```

#### `gmail.drafts.update`

- **Method:** `PUT`
- **URL:** `/api/v1/gmail/drafts/update`
- **Google path:** `/gmail/v1/users/me/drafts/{draftId}`
- **Enabled by default:** `No`

Edit an existing draft

```json
{
  "properties": {
    "body": {
      "description": "Updated email body text",
      "type": "string"
    },
    "draft_id": {
      "description": "The ID of the draft to update",
      "type": "string"
    },
    "subject": {
      "description": "Updated email subject line",
      "type": "string"
    },
    "to": {
      "description": "Updated recipient email addresses",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "draft_id"
  ],
  "type": "object"
}
```

#### `gmail.drafts.send`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/drafts/send`
- **Google path:** `/gmail/v1/users/me/drafts/send`
- **Enabled by default:** `No`
- **Default policy:** `{"max_recipients": 5}`

Send an existing draft (separate from messages.send)

```json
{
  "properties": {
    "draft_id": {
      "description": "The ID of the draft to send",
      "type": "string"
    }
  },
  "required": [
    "draft_id"
  ],
  "type": "object"
}
```

#### `gmail.drafts.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/gmail/drafts/delete`
- **Google path:** `/gmail/v1/users/me/drafts/{draftId}`
- **Enabled by default:** `No`

Delete a draft

```json
{
  "properties": {
    "draft_id": {
      "description": "The ID of the draft to delete",
      "type": "string"
    }
  },
  "required": [
    "draft_id"
  ],
  "type": "object"
}
```

#### `gmail.labels.list`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/labels/list`
- **Google path:** `/gmail/v1/users/me/labels`
- **Enabled by default:** `Yes`

List all labels in the user's mailbox

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.labels.get`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/labels/get`
- **Google path:** `/gmail/v1/users/me/labels/{labelId}`
- **Enabled by default:** `Yes`

Get details of a specific label

```json
{
  "properties": {
    "label_id": {
      "description": "The ID of the label to retrieve",
      "type": "string"
    }
  },
  "required": [
    "label_id"
  ],
  "type": "object"
}
```

#### `gmail.filters.list`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/filters/list`
- **Google path:** `/gmail/v1/users/me/settings/filters`
- **Enabled by default:** `No`

List all email filters

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.filters.get`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/filters/get`
- **Google path:** `/gmail/v1/users/me/settings/filters/{filterId}`
- **Enabled by default:** `No`

Get details of a specific filter

```json
{
  "properties": {
    "filter_id": {
      "description": "The ID of the filter to retrieve",
      "type": "string"
    }
  },
  "required": [
    "filter_id"
  ],
  "type": "object"
}
```

#### `gmail.filters.create`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/filters/create`
- **Google path:** `/gmail/v1/users/me/settings/filters`
- **Enabled by default:** `No`

Create a new email filter with criteria and actions

```json
{
  "properties": {
    "archive": {
      "description": "Whether to archive matching messages (skip inbox)",
      "type": "boolean"
    },
    "delete": {
      "description": "Whether to send matching messages to trash",
      "type": "boolean"
    },
    "forward": {
      "description": "Email address to forward matching messages to",
      "type": "string"
    },
    "label_ids": {
      "description": "Label IDs to apply to matching messages",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "mark_as_important": {
      "description": "Whether to mark matching messages as important",
      "type": "boolean"
    },
    "mark_as_read": {
      "description": "Whether to mark matching messages as read",
      "type": "boolean"
    },
    "query": {
      "description": "Gmail search query for the filter criteria (e.g., 'from:alice@example.com')",
      "type": "string"
    },
    "star": {
      "description": "Whether to star matching messages",
      "type": "boolean"
    }
  },
  "required": [
    "query"
  ],
  "type": "object"
}
```

#### `gmail.filters.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/gmail/filters/update`
- **Google path:** `/gmail/v1/users/me/settings/filters/{filterId}`
- **Enabled by default:** `No`

Update an existing filter's criteria or actions

```json
{
  "properties": {
    "archive": {
      "description": "Whether to archive matching messages",
      "type": "boolean"
    },
    "filter_id": {
      "description": "The ID of the filter to update",
      "type": "string"
    },
    "label_ids": {
      "description": "New label IDs to apply",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "mark_as_important": {
      "description": "Whether to mark matching messages as important",
      "type": "boolean"
    },
    "mark_as_read": {
      "description": "Whether to mark matching messages as read",
      "type": "boolean"
    },
    "query": {
      "description": "New Gmail search query for the filter criteria",
      "type": "string"
    }
  },
  "required": [
    "filter_id"
  ],
  "type": "object"
}
```

#### `gmail.filters.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/gmail/filters/delete`
- **Google path:** `/gmail/v1/users/me/settings/filters/{filterId}`
- **Enabled by default:** `No`

Delete an email filter

```json
{
  "properties": {
    "filter_id": {
      "description": "The ID of the filter to delete",
      "type": "string"
    }
  },
  "required": [
    "filter_id"
  ],
  "type": "object"
}
```

#### `gmail.labels.create`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/labels/create`
- **Google path:** `/gmail/v1/users/me/labels`
- **Enabled by default:** `No`

Create a new custom label

```json
{
  "properties": {
    "label_list_visibility": {
      "default": "labelShow",
      "description": "Whether the label is visible in the label list: 'labelShow' or 'labelHide'",
      "type": "string"
    },
    "message_list_visibility": {
      "default": "show",
      "description": "Whether messages with this label are visible in the message list: 'show' or 'hide'",
      "type": "string"
    },
    "name": {
      "description": "Display name of the label",
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "type": "object"
}
```

#### `gmail.labels.update`

- **Method:** `PATCH`
- **URL:** `/api/v1/gmail/labels/update`
- **Google path:** `/gmail/v1/users/me/labels/{labelId}`
- **Enabled by default:** `No`

Update a label (rename, change visibility, color)

```json
{
  "properties": {
    "label_id": {
      "description": "The ID of the label to update",
      "type": "string"
    },
    "label_list_visibility": {
      "description": "Whether the label is visible in the label list: 'labelShow' or 'labelHide'",
      "type": "string"
    },
    "message_list_visibility": {
      "description": "Whether messages with this label are visible in the message list: 'show' or 'hide'",
      "type": "string"
    },
    "name": {
      "description": "New display name for the label",
      "type": "string"
    }
  },
  "required": [
    "label_id"
  ],
  "type": "object"
}
```

#### `gmail.labels.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/gmail/labels/delete`
- **Google path:** `/gmail/v1/users/me/labels/{labelId}`
- **Enabled by default:** `No`

Delete a custom label

```json
{
  "properties": {
    "label_id": {
      "description": "The ID of the label to delete",
      "type": "string"
    }
  },
  "required": [
    "label_id"
  ],
  "type": "object"
}
```

#### `gmail.messages.untrash`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/untrash`
- **Google path:** `/gmail/v1/users/me/messages/{messageId}/untrash`
- **Enabled by default:** `No`

Untrash a message (recover from trash)

```json
{
  "properties": {
    "message_id": {
      "description": "The ID of the message to untrash",
      "type": "string"
    }
  },
  "required": [
    "message_id"
  ],
  "type": "object"
}
```

#### `gmail.messages.batch_modify`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/batch_modify`
- **Google path:** `/gmail/v1/users/me/messages/batchModify`
- **Enabled by default:** `No`

Modify labels on multiple messages at once

```json
{
  "properties": {
    "add_label_ids": {
      "description": "Label IDs to add",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "ids": {
      "description": "List of message IDs to modify",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "remove_label_ids": {
      "description": "Label IDs to remove",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "ids"
  ],
  "type": "object"
}
```

#### `gmail.messages.batch_delete`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/batch_delete`
- **Google path:** `/gmail/v1/users/me/messages/batchDelete`
- **Enabled by default:** `No`

Delete multiple messages at once

```json
{
  "properties": {
    "ids": {
      "description": "List of message IDs to delete",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "ids"
  ],
  "type": "object"
}
```

#### `gmail.messages.attachments.get`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/messages/attachments/get`
- **Google path:** `/gmail/v1/users/me/messages/{messageId}/attachments/{attachmentId}`
- **Enabled by default:** `Yes`

Download a message attachment

```json
{
  "properties": {
    "attachment_id": {
      "description": "The ID of the attachment",
      "type": "string"
    },
    "message_id": {
      "description": "The ID of the message",
      "type": "string"
    }
  },
  "required": [
    "message_id",
    "attachment_id"
  ],
  "type": "object"
}
```

#### `gmail.threads.list`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/threads/list`
- **Google path:** `/gmail/v1/users/me/threads`
- **Enabled by default:** `Yes`

List email threads (conversations)

```json
{
  "properties": {
    "label_ids": {
      "description": "Label IDs to filter by",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "max_results": {
      "default": 20,
      "description": "Maximum number of threads to return",
      "type": "integer"
    },
    "q": {
      "description": "Gmail search query",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `gmail.threads.get`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/threads/get`
- **Google path:** `/gmail/v1/users/me/threads/{threadId}`
- **Enabled by default:** `Yes`

Get a thread with all its messages

```json
{
  "properties": {
    "format": {
      "default": "full",
      "description": "Format: full, minimal, raw, metadata",
      "type": "string"
    },
    "thread_id": {
      "description": "The ID of the thread",
      "type": "string"
    }
  },
  "required": [
    "thread_id"
  ],
  "type": "object"
}
```

#### `gmail.threads.modify`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/threads/modify`
- **Google path:** `/gmail/v1/users/me/threads/{threadId}/modify`
- **Enabled by default:** `No`

Modify labels on all messages in a thread

```json
{
  "properties": {
    "add_label_ids": {
      "description": "Label IDs to add",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "remove_label_ids": {
      "description": "Label IDs to remove",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "thread_id": {
      "description": "The ID of the thread",
      "type": "string"
    }
  },
  "required": [
    "thread_id"
  ],
  "type": "object"
}
```

#### `gmail.threads.trash`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/threads/trash`
- **Google path:** `/gmail/v1/users/me/threads/{threadId}/trash`
- **Enabled by default:** `No`

Trash a thread and all its messages

```json
{
  "properties": {
    "thread_id": {
      "description": "The ID of the thread to trash",
      "type": "string"
    }
  },
  "required": [
    "thread_id"
  ],
  "type": "object"
}
```

#### `gmail.threads.untrash`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/threads/untrash`
- **Google path:** `/gmail/v1/users/me/threads/{threadId}/untrash`
- **Enabled by default:** `No`

Untrash a thread

```json
{
  "properties": {
    "thread_id": {
      "description": "The ID of the thread to untrash",
      "type": "string"
    }
  },
  "required": [
    "thread_id"
  ],
  "type": "object"
}
```

#### `gmail.threads.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/gmail/threads/delete`
- **Google path:** `/gmail/v1/users/me/threads/{threadId}`
- **Enabled by default:** `No`

Permanently delete a thread

```json
{
  "properties": {
    "thread_id": {
      "description": "The ID of the thread to permanently delete",
      "type": "string"
    }
  },
  "required": [
    "thread_id"
  ],
  "type": "object"
}
```

#### `gmail.history.list`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/history/list`
- **Google path:** `/gmail/v1/users/me/history`
- **Enabled by default:** `Yes`

List mailbox history (incremental sync)

```json
{
  "properties": {
    "label_id": {
      "description": "Only return history for this label",
      "type": "string"
    },
    "max_results": {
      "default": 100,
      "description": "Maximum number of history records to return",
      "type": "integer"
    },
    "start_history_id": {
      "description": "Starting history ID for the listing",
      "type": "string"
    }
  },
  "type": "object"
}
```

#### `gmail.settings.forwarding_addresses.list`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/settings/forwarding_addresses/list`
- **Google path:** `/gmail/v1/users/me/settings/forwardingAddresses`
- **Enabled by default:** `No`

List forwarding addresses

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.forwarding_addresses.get`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/settings/forwarding_addresses/get`
- **Google path:** `/gmail/v1/users/me/settings/forwardingAddresses/{forwardingEmail}`
- **Enabled by default:** `No`

Get a forwarding address

```json
{
  "properties": {
    "forwarding_email": {
      "description": "The forwarding email address",
      "type": "string"
    }
  },
  "required": [
    "forwarding_email"
  ],
  "type": "object"
}
```

#### `gmail.settings.forwarding_addresses.create`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/settings/forwarding_addresses/create`
- **Google path:** `/gmail/v1/users/me/settings/forwardingAddresses`
- **Enabled by default:** `No`

Create a forwarding address

```json
{
  "properties": {
    "forwarding_email": {
      "description": "The email address to forward to",
      "type": "string"
    }
  },
  "required": [
    "forwarding_email"
  ],
  "type": "object"
}
```

#### `gmail.settings.forwarding_addresses.delete`

- **Method:** `DELETE`
- **URL:** `/api/v1/gmail/settings/forwarding_addresses/delete`
- **Google path:** `/gmail/v1/users/me/settings/forwardingAddresses/{forwardingEmail}`
- **Enabled by default:** `No`

Delete a forwarding address

```json
{
  "properties": {
    "forwarding_email": {
      "description": "The forwarding email address to delete",
      "type": "string"
    }
  },
  "required": [
    "forwarding_email"
  ],
  "type": "object"
}
```

#### `gmail.get_profile`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/get_profile`
- **Google path:** `/gmail/v1/users/me/profile`
- **Enabled by default:** `Yes`

Gets the current user's Gmail profile.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.watch`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/watch`
- **Google path:** `/gmail/v1/users/me/watch`
- **Enabled by default:** `No`

Set up or update a push notification watch on the given user mailbox.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.stop`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/stop`
- **Google path:** `/gmail/v1/users/me/stop`
- **Enabled by default:** `No`

Turn off push notification delivery for the given user mailbox.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.messages.import`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/import`
- **Google path:** `/gmail/v1/users/me/messages/import`
- **Query params:** `deleted`, `internalDateSource`, `neverMarkSpam`, `processForCalendar`
- **Enabled by default:** `No`

Imports a message into only this user's mailbox.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.messages.insert`

- **Method:** `POST`
- **URL:** `/api/v1/gmail/messages/insert`
- **Google path:** `/gmail/v1/users/me/messages`
- **Query params:** `deleted`, `internalDateSource`
- **Enabled by default:** `No`

Directly inserts a message into only this user's mailbox.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.labels.patch`

- **Method:** `PATCH`
- **URL:** `/api/v1/gmail/labels/patch`
- **Google path:** `/gmail/v1/users/me/labels/{id}`
- **Enabled by default:** `No`

Patch the specified label.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.get_imap`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/settings/get_imap`
- **Google path:** `/gmail/v1/users/me/settings/imap`
- **Enabled by default:** `Yes`

Gets IMAP settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.update_imap`

- **Method:** `PUT`
- **URL:** `/api/v1/gmail/settings/update_imap`
- **Google path:** `/gmail/v1/users/me/settings/imap`
- **Enabled by default:** `No`

Updates IMAP settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.get_pop`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/settings/get_pop`
- **Google path:** `/gmail/v1/users/me/settings/pop`
- **Enabled by default:** `Yes`

Gets POP settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.update_pop`

- **Method:** `PUT`
- **URL:** `/api/v1/gmail/settings/update_pop`
- **Google path:** `/gmail/v1/users/me/settings/pop`
- **Enabled by default:** `No`

Updates POP settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.get_vacation`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/settings/get_vacation`
- **Google path:** `/gmail/v1/users/me/settings/vacation`
- **Enabled by default:** `Yes`

Gets vacation responder settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.update_vacation`

- **Method:** `PUT`
- **URL:** `/api/v1/gmail/settings/update_vacation`
- **Google path:** `/gmail/v1/users/me/settings/vacation`
- **Enabled by default:** `No`

Updates vacation responder settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.get_language`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/settings/get_language`
- **Google path:** `/gmail/v1/users/me/settings/language`
- **Enabled by default:** `Yes`

Gets language settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.update_language`

- **Method:** `PUT`
- **URL:** `/api/v1/gmail/settings/update_language`
- **Google path:** `/gmail/v1/users/me/settings/language`
- **Enabled by default:** `No`

Updates language settings.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.get_auto_forwarding`

- **Method:** `GET`
- **URL:** `/api/v1/gmail/settings/get_auto_forwarding`
- **Google path:** `/gmail/v1/users/me/settings/autoForwarding`
- **Enabled by default:** `Yes`

Gets the auto-forwarding setting for the specified account.

```json
{
  "properties": {},
  "type": "object"
}
```

#### `gmail.settings.update_auto_forwarding`

- **Method:** `PUT`
- **URL:** `/api/v1/gmail/settings/update_auto_forwarding`
- **Google path:** `/gmail/v1/users/me/settings/autoForwarding`
- **Enabled by default:** `No`

Updates the auto-forwarding setting for the specified account.

```json
{
  "properties": {},
  "type": "object"
}
```

---

Generated at: `2026-07-08T17:30:43.366244+00:00Z`
Source commit: `6930b3984e363fae74c0fde65f3f9e4e9e70d01e`
